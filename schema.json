{
  "type": "object",
  "properties": {
    "url": {
      "type": "string",
      "description": "Override URL. Gebruik {assetId} als placeholder. Indien leeg wordt de standaard signing URL gebruikt."
    }
  }
}